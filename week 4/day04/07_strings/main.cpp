#include <iostream>

// Given a string, compute recursively (no loops) a new string where all the
// lowercase 'x' chars have been changed to 'y' chars.

std::string something = "xyxyxyxyxyxyxy";

std::string changer (something){
    if ( something == x ){
        return y;
    }else if ( something == y){
        return y;
    }
}

int main() {

    std::cout << charger << std::endl;
    return 0;
}


